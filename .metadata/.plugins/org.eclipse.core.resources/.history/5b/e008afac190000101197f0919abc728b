/* Đặt nền và căn giữa form đăng ký */
body {
    background-color: #f4f4f4; /* Màu nền của trang */
    font-family: Arial, sans-serif;
    margin: 0;
    padding: 0;
    display: flex;
    justify-content: center; /* Căn giữa theo chiều ngang */
    align-items: center;     /* Căn giữa theo chiều dọc */
    height: 100vh;           /* Chiều cao toàn màn hình */
}

/* Khung chứa form đăng ký */
.form-container {
    background-color: #ffffff; /* Nền trắng cho form */
    padding: 30px;
    border: 1px solid #ddd;
    border-radius: 8px;
    box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
    width: 400px;
}

/* Tiêu đề form */
.form-container h2 {
    text-align: center;
    color: #333;
    margin-bottom: 20px;
}

/* Định dạng cho nhãn (label) */
.form-container label {
    display: block;
    font-weight: bold;
    margin-bottom: 5px;
    color: #555;
}

/* Định dạng cho các trường nhập liệu và textarea */
.form-container input[type="text"],
.form-container input[type="email"],
.form-container input[type="password"],
.form-container textarea {
    width: 100%;
    padding: 10px;
    margin-bottom: 15px;
    border: 1px solid #ccc;
    border-radius: 4px;
    box-sizing: border-box;
    font-size: 14px;
}

/* Nút submit */
.form-container button {
    display: block;
    width: 100%;
    background-color: #007bff;
    color: #fff;
    padding: 10px;
    border: none;
    border-radius: 4px;
    font-size: 16px;
    cursor: pointer;
    transition: background-color 0.3s ease;
}

.form-container button:hover {
    background-color: #0056b3;
}

/* Định dạng cho đoạn text chứa link đăng nhập */
.form-container p {
    text-align: center;
    margin-top: 20px;
}

.form-container p a {
    color: #007bff;
    text-decoration: none;
}

.form-container p a:hover {
    text-decoration: underline;
}
